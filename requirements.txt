# Runtime dependencies outside Houdini's bundled Python modules.
httpx>=0.27
mcp>=1.0
numpy>=1.24
tiktoken>=0.7
websockets>=12

# Houdini 21 bundles PySide6 for panel execution. Install locally only for
# standalone UI development/tests outside Houdini.
PySide6>=6.6

# Optional local speech-to-text panel extras:
#   pip install -e ".[asr]"
